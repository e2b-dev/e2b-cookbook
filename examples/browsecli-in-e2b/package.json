{
  "name": "browsecli-in-e2b",
  "version": "1.0.0",
  "description": "Run a browser agent (Vercel AI SDK) inside an E2B sandbox that drives a remote Browserbase browser over CDP via the `browse` CLI.",
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "start": "tsx index.ts",
    "build:template": "e2b template build"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "e2b": "^2.3.4"
  },
  "devDependencies": {
    "@types/node": "^20.16.6",
    "tsx": "^4.7.3",
    "typescript": "^5.4.5"
  },
  "keywords": [
    "e2b",
    "browserbase",
    "browse-cli",
    "sandbox",
    "cdp",
    "agent",
    "ai-sdk"
  ],
  "license": "MIT"
}
