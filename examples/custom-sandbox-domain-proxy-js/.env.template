# TODO: Get your E2B API key from https://e2b.dev/docs/getting-started/api-key
E2B_API_KEY=""

# Port for the proxy server (default: 80)
PORT=80

# Port inside the sandbox where Python HTTP server runs (default: 8000)
PORT_IN_SANDBOX=8000
