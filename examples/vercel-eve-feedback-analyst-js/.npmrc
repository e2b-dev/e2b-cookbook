# @e2b/eve-sandbox 0.1.1 declares a peer of eve@^0.27.0, which predates the eve
# release this example is written against (and every eve release since). The
# peer range is stale rather than a real incompatibility, so install past it.
# Remove this once @e2b/eve-sandbox widens its peer range.
legacy-peer-deps=true
